{% extends 'block.twig' %}

{% block content %}
	<div>
		<div class="container">
			<div class="row">
				<div class="col-xs-12">
					{% if fields.tagline %}
						<h2>{{fields.tagline}}</h2>
					{% endif %}
				</div>
			</div>
		</div>
	</div>
{% endblock %}
