<img class="{{class}}" src="{{ Image(image).src(size ? size : 'medium') }}" srcset="{{ Image(image).srcset }}" sizes="{{ Image(image).img_sizes }}" alt="{{image.alt}}" {{title ? "title='#{title}'"}}>
